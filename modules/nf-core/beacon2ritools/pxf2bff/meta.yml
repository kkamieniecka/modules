---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/yaml-schema.json
name: "pxf2bff"
## TODO nf-core: Add a description of the module and list keywords
description:  module that converts Phenopacket PXF (JSON) to BFF (JSON)
keywords:
  - beacon
  - phenopacket
  - cnv
tools:
  - "beacon2ritools":
      ## TODO nf-core: Add a description and other details for the software below
      description: "Script to transform genomic variations data (VCF) to queryable data (MongoDB)."
      homepage: "None"
      documentation: "https://b2ri-documentation.readthedocs.io/en/latest/data-ingestion/"
      tool_dev_url: "None"
      doi: ""
      licence: "['GPL v3']"

## TODO nf-core: Add a description of all of the variables used as input
input:
  # Only when we have meta
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. `[ id:'test', single_end:false ]`
  
  ## TODO nf-core: Delete / customise this example input
  - json:
      type: file
      description: Phenopacket JSON files
      pattern: "*.{json}"

## TODO nf-core: Add a description of all of the variables used as output
output:
  #Only when we have meta
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. `[ id:'test', single_end:false ]`
  
  - versions:
      type: file
      description: File containing software versions
      pattern: "versions.yml"
  ## TODO nf-core: Delete / customise this example output
  - bff:
      type: file
      description: BFF files
      pattern: "*.{bff}"

authors:
  - "@kkamieniecka"
